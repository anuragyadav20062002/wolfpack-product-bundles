/**
 * Shopify API Version Constants
 *
 * Centralized API version strings used in REST/Storefront API URLs.
 * The Admin GraphQL API version is managed by the Shopify SDK (ApiVersion enum).
 */

export const SHOPIFY_REST_API_VERSION = "2025-01";
