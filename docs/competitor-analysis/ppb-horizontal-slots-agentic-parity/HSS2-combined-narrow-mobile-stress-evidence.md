# HSS2 Combined Narrow-Mobile Stress Evidence

Date: 2026-07-13

## Matrix

The 360 x 800 stress pass combined long content, grouped variant `8`, a sole
inventory survivor, multi-step persistence, 5% and 10% discount tiers, internal
modal scrolling, final slot order, CTA enablement, and document overflow. EB was
inspected first, followed by WPB widget `5.0.158`.

## EB

Step 1 selected `14k Dangling Obsidian Earrings` and `18k Pedal Ring / 8`.
The footer showed `₹1228` original, `₹1166.60` discounted, and count `2`. The
modal body remained internally scrollable at `970px` content height inside a
`533px` client height, with no horizontal scroll.

Step 2 selected `Massage Oil / Grapefruit`, the sole sellable survivor in the
equivalent EB category. Done returned the three variants in order. The final CTA
displayed `₹1127.70` and `10% off`, and the document had no horizontal overflow.

## WPB

The equivalent Step 1 pass selected `14k Dangling Obsidian Earrings` and
`18k Pedal Ring / 8`. Step 2 selected the sole live `Selling Plans Ski Wax /
Special Selling Plans Ski Wax` survivor.

Done returned variant IDs `48720141091075`, `48720161210627`, and
`48720397271299` in slot order. The final CTA was enabled and displayed
`Add Bundle to Cart • $1150.15`. The document width matched its 360px client
width with zero horizontal overflow.

## Cleanup

After evidence capture, all three selected items were removed from each live
fixture. EB returned to Step 1 slots `Product 1`, `Product 2`, Step 2 slot
`Product 1`; WPB returned to the equivalent empty two-plus-one slot state.

## Result

HSS2 is accepted. The narrowest required viewport preserves grouped identity,
inventory filtering, discount progression, multi-step state, slot ordering,
reachable final action, and horizontal containment under one combined pass.
